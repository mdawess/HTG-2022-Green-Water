---
title: "hack the globe"
author: "Nyanda Redwood"
date: "19/03/2022"
output:
    pdf_document:
            latex_engine: xelatex
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# load necessary libraries
library(MPV)
library(tidyverse)

# load the data
percip <- read.csv("percip_brazil.csv")

#group by year
percip <- percip |> group_by(year)

# get the response variable - value(which is the precipitation), and the predictor - year
x <- percip$year
y <- percip$value

#plot(x, y)
par(mfrow=c(1,2))
par(mfrow=c(2,2), mar=c(4, 4, 2, 1))

# Run a generalized linear model on it using poisson to predict precipitation
y.glm <- glm(y ~ x, family = poisson(link = "log"))

summary(y.glm)
plot(y.glm)
```
The p-value is highly significant and we have very low standard error. So, we fail to reject the null hypothesis.  
  
The formula to predict rainfall is:  
  
- precipitation = -33.5 + 0.02*year. 
